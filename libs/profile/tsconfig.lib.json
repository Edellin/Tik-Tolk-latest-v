{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // 📁 Куда собирать результат
    "outDir": "../../dist/out-tsc",

    // 📘 Генерация деклараций для экспорта типов
    "declaration": true,
    "declarationMap": true,

    // 🧭 Карты исходников — полезно при отладке
    "sourceMap": true,
    "inlineSources": true,

    // 📦 Не подключаем лишние глобальные типы (например, Node)
    "types": [],

    // ⚙️ Повышаем стабильность компиляции
    "stripInternal": true,
    "noEmitOnError": true
  },
  "exclude": [
    "src/**/*.spec.ts",
    "src/**/*.test.ts",
    "jest.config.ts"
  ],
  "include": [
    "src/**/*.ts"
  ]
}
